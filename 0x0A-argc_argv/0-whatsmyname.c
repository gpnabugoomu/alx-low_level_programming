#include "main.h"
#include <stdio.h>

/**
 * main - function that pronts its name
 * @argc: the argument count
 * @argv: the argument vector
 *
 * Return: Always zero
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
